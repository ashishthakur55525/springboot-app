apiVersion: v1
kind: ConfigMap
metadata:
  name: springboot-app-cm
commonAnnotations:
  sample-version: v1
data:
  initdb.sql: |
    CREATE DATABASE football;
    CREATE TABLE `squadeeeeeeeee` (
        `name` varchar(15) PRIMARY KEY NOT NULL
    );
    USE football;
    ...